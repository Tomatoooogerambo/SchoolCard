CREATE TABLE manager (
    ma_jobnumber VARCHAR(10) NOT NULL,
    ma_password VARCHAR(10) NOT NULL,
    school_district VARCHAR(10) NOT NULL,
    ma_position VARCHAR(20) NOT NULL,
    PRIMARY KEY(ma_jobnumber)
);